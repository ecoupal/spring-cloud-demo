spring.application.name: config
server.port: 8888

eureka:
  instance.preferIpAddress: true
  client:
    serviceUrl.defaultZone: http://${EUREKA_SERVICE_URL:localhost}:8761/eureka/

spring:
  cloud.config.server.git.uri: classpath:/config-repo
  profiles.active: native